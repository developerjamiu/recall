name: recall_data
description: Data layer abstractions for Recall - repository interfaces and service contracts
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  common:
  dart_jsonwebtoken: ^3.2.0
  dotenv: ^4.2.0
  http: ^1.5.0
  jao: ^0.3.0
  uuid: ^4.5.1

dev_dependencies:
  build_runner: ^2.7.0
  jao_cli: ^0.3.0
  jao_generator: ^0.3.0
  lints: ^6.0.0
  mocktail: ^1.0.4
  test: ^1.25.0

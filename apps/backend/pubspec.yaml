name: backend
description: Recall API — Dart Frog backend with Jao ORM and PostgreSQL.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ^3.9.2

resolution: workspace

dependencies:
  common:
  crypto: ^3.0.6
  dart_frog: ^1.2.3
  dart_frog_auth: ^1.2.1
  http: ^1.5.0
  jao: ^0.3.1
  recall_data:
  shelf_cors_headers: ^0.1.5

dev_dependencies:
  mocktail: ^1.0.3
  test: ^1.25.5
  very_good_analysis: ^5.1.0
